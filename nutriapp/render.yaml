services:
  - type: web
    name: nutriapp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: NOTION_TOKEN
        sync: false
      - key: SECRET_KEY
        generateValue: true
